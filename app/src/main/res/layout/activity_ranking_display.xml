<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/caveira_do_rock"
    android:padding="16dp"
    tools:context=".RankingDisplayActivity">

    <TextView
        android:id="@+id/title_ranking"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:text="Ranking 🏆"
        android:textColor="@android:color/holo_purple"
        android:textSize="22sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/title_score"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:text="Pontuação"
        android:textColor="@android:color/holo_purple"
        android:textSize="22sp"
        android:textStyle="bold" />

    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:layout_below="@id/title_ranking"
        android:layout_marginTop="8dp"
        android:background="@android:color/holo_purple" />

    <Button
        android:id="@+id/button_back"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="Voltar" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_ranking_display"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/divider"
        android:layout_above="@id/button_back"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp" />

</RelativeLayout>
